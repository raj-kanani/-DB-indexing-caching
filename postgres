how to use postgresql  database in django  :

- curl https://www.pgadmin.org/static/packages_pgadmin_org.pub | sudo apt-key add -
- sudo snap install curl 
- sudo sh -c 'echo "deb https://ftp.postgresql.org/pub/pgadmin/pgadmin4/apt/focal pgadmin4 main" > /etc/apt/sources.list.d/pgadmin4.list' 
- sudo apt update
- sudo apt install pgadmin4 
- sudo /usr/pgadmin4/bin/setup-web.sh 
- pip install psycopg2
- pip install psycopg2-binary


### connect db table with postgresql :
- sudo systemctl start postgresql.service
- sudo -i -u postgres
- psql 

## Types of Database Indexing :
				1) BloomIndex.
				2) BrinIndex.
				3) BTreeIndex.
				4) GinIndex.
				5) GistIndex.
				6) HashIndex.
				7) SpGistIndex.
				 	

# QUERY :

- \ds
- CREATE INDEX (index-name) ON table-name(fieldname);
- explain select * from table_name where field_name =’any_field_data’
- select * from table-name where field-name='fieldname-value'
- drop table table-name
- SELECT COUNT(*) FROM tablename WHERE fieldname='fieldvalue'






